---
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  release:
    uses: "Mosher-Labs/.github/.github/workflows/release.yml@main"
    secrets: inherit
    permissions:
      contents: write
      issues: write
      pull-requests: write
permissions: read-all
